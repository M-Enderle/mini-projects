services:
  mini-projects:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - kleinanzeigen_data:/app/kleinanzeigen-map
      - fuel_tracker_data:/app/fuel-tracker
      - recipe_book_data:/app/recipe-book

volumes:
  kleinanzeigen_data:
  fuel_tracker_data:
  recipe_book_data:
